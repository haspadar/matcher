<?xml version="1.0"?>
<ruleset name="Matcher Project">
    <description>PHP_CodeSniffer rules for Matcher project.</description>

    <files>
        <directory>src</directory>
        <directory>tests</directory>
    </files>

    <rule ref="PSR12"/>

    <!-- Игнорировать папку с кешем -->
    <exclude-pattern>var/*</exclude-pattern>
    <rule ref="SlevomatCodingStandard.Namespaces.UnusedUses"/>

</ruleset>